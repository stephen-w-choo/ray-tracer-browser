// import { makePPM } from "../../src/bunUtils/makePPM"
// import { Canvas } from "../../src/types/Canvas"
// import { createColor } from "../../src/types/Color"

const OUTPUT_FILE_PATH = "output_ppm/"

// const canvas = new Canvas(3, 5)

// const c1 = createColor(1.5, 0, 0)
// const c2 = createColor(0, 0.5, 0)
// const c3 = createColor(-0.5, 0, 1)

// canvas.pixel.set(0, 0, c1)
// canvas.pixel.set(1, 2, c2)
// canvas.pixel.set(2, 4, c3)

// const ppm = makePPM(canvas, OUTPUT_FILE_PATH + "test.ppm")



// const canvas2 = new Canvas(2, 10)
// const c = createColor(1, 0.8, 0.6)

// // When
// for (let y = 0; y < canvas2.height; y++) {
//     for (let x = 0; x < canvas2.width; x++) {
//         canvas2.pixel.set(y, x, c)
//     }
// }

// makePPM(canvas2, OUTPUT_FILE_PATH + "test2.ppm")